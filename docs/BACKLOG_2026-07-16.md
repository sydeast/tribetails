# AuntieOS Consolidated Backlog — 2026-07-16

**This file is the single source of truth for AuntieOS open work.** It reconciles
six prior tracking docs that each used their own ID scheme with no mapping
between them, so the same defect was logged up to three times under three IDs
(e.g. the `serviceTone` "sit"-in-"visit" bug was `web-audit`, `punchlist B7`, and
`AO-19`). One namespace now: **AO-###** (operational items keep **O-##**).

Method: five parallel read-only agents extracted the still-open items from each
source doc and **re-verified every one against current source** (prior recon was
repeatedly wrong). Contested items were re-checked by hand. Counts below are the
verified reality, which is far more resolved than the stale docs claimed.

## The six retired docs → this one

| Retired doc | Scheme | Doc's count | Verified done/open | Status |
|---|---|---|---|---|
| `docs/2026-06-01-web-audit.md` | untagged | ~170 | ~150 done / ~16 open | **DEPRECATED** → here |
| `docs/2026-06-01-redesign-backlog.md` | flag/method | ~64 | ~53 done / 11 open | **DEPRECATED** → here |
| `AuntieOS_Fix_Backlog_2026-06-02.md` | `[ ]` phases | 81 | ~72 done / 9 open | **DEPRECATED** → here |
| `docs/punchlists/PUNCHLIST_2026-06-23-A8-testing.md` | `B*/N*/K*/A*/W*` | ~40 | ~26 done / ~17 open | **DEPRECATED** → here |
| `docs/reviews/CODE_REVIEW_2026-06-18.md` | `WARNING-*/NOTE-*` | 58 | 47 done / 11 open | **DEPRECATED** → here |
| `docs/AUNTIEOS_DEVELOPMENT_PLAN_2026-07-16.md` (Open-items registry) | `AO-*/O-*` | 20+ | see below | plan STAYS; its registry section moves here |

Prior aggregate claim was "~335 items logged, true open unmeasurable". Verified
open, deduped: **~55 items**, of which only ~12 are permanent code bugs; the rest
are wasm-throwaway (the wasm admin is deleted at the React cutover "A8"),
data/backfill coverage, by-design, or blocked on an operator/external action.

---

## 0c. Closed 2026-07-17 (backend + polish push) — do not re-log

| AO | Outcome | Evidence |
|---|---|---|
| AO-28 | **DONE** — notifications carry `title`/`description` (catalog label/desc) + resolved `actorName`/`actorPhotoUrl` (`resolveActor`: staff→kinfolk, best-effort); React feed renders them | MyTribe `3fea901`, admin `36f7629` (admin deployed); +4 resolveActor + dispatcher/React tests |
| AO-27 | **BY-DESIGN (not a bug)** — the notifications rule grants admin an unfiltered read (admin console lists all dispatches); React stream is capped at 200 (AO-29). Documented in `api/notifications.ts`. | admin `api/notifications.ts` |
| AO-32 | **DONE** — `getTrainingDoc`/`getDraft` no longer return the whole doc (leaked raw notes/PII/attachments); field-projected to what n8n reads; auth gate untouched | AuntieOS `4c6eca9`; 40 tests |
| AO-31 | **DONE** — reconcile prompt emits undated `[[source: <ch>]]` (was dated); `count_sources` still keys off the marker | AuntieOS `4c6eca9` (file gitignored, on disk) |
| AO-7 | **DONE** — handler-level tests for setAdminClaim/signCloudinaryUpload/searchMapbox/sendMessage (was: pure-helpers only). 25 tests, zero source change, auth gates pinned | AuntieOS `99e4d55`; suite 102 pass |
| AO-16 | **DONE** — glass rim white halo in dark theme dampened; both Compose surfaces (desktop + android) | AuntieOS `e906283`; both compiles green |
| React detail: Inbox thread | **DONE** — the Inbox `onSelectThread` seam now opens a `ConversationThread` view (read via getConversationThread, reply via replyToConversation, day-grouped bubbles, fail-loud) | admin `80a4789` (deployed); +6 tests |
| React detail: KinfolkProfile (+AO-48) | **DONE** — Directory `onSelectKinfolk` opens a full household profile (Contact/Home/Emergency/Vet/Kin sections, one-shot full-doc read, all-blank sections omitted); addresses AO-48 (not one scroll) | admin `2acf453` (deployed); +7 tests |
| AO-21 | **OPERATOR-BLOCKED** — secure flip loses inbound call/vm/sms until Twilio inbound webhooks are live (WARNING-8). No safe engineering-only completion. | — |
| AO-23 | **DEFERRED (unconfirmed)** — Compose child clickable wins over parent, so likely not broken; needs live-drive; blind change risks regressing every entity row. | — |
| AO-5 / AO-8 | **ARCHITECTURAL (deferred)** — one brand-voice source + shared contract types need a shared package spanning TWO separately-deployed function codebases (AuntieOS web/functions + MyTribe functions, different repos). Cross-repo design pass, risks the live generate/personalize flow. | — |

---

## 0b. Closed 2026-07-17 (React-port yellow verify + AO-25) — do not re-log

Verified all 15 flagged "React-port" items against `auntieos-admin` source; fixed
what was genuinely open, and built the two real gaps (AO-56, AO-25). Both repos
green (admin 1533 tests, functions 1470+), tsc clean, deployed live.

| AO | Outcome | Evidence |
|---|---|---|
| AO-44 | **FIXED** — AddKinfolkDialog + AddKinDialog now reveal each required-field error on BLUR (per-field touched), not only on save | admin `be39cae`; +2 tests |
| AO-49 | **FIXED** — FormSchemaEditor auto-derives a field key from the label on blur (`deriveFieldKey`, camelCase, never clobbers a typed key) | admin `be39cae`; +5 tests |
| AO-56 | **DONE — all four surfaces** — `unassignTemplate` callable + React admin TemplateAssignments screen + Compose desktop/wasm TemplateAssignmentScreen Unassign action + android TemplateAssignmentScreen Unassign action | MyTribe `1c8aa1a` (deployed), React admin `be39cae`, android `2ff604a`, Compose desktop/wasm `fd6e41b`; +14 tests |
| AO-25 | **DONE — all four surfaces** — `createMultiDateBookingRequest` admin callable (reuses requestBooking's envelope writer) + React NewBookingDialog + Compose desktop/wasm BookingCreateScreen Repeat control + android NewBookingRequestDialog (non-consecutive dates OR weekly recurrence) | MyTribe `95c7e68` (deployed), React admin `105eb95`, android `2ff604a`, Compose desktop/wasm `fd6e41b`; +34 tests |
| AO-11 | **CLOSED by construction** — KinfolkCard has no fixed height / no overflow:hidden; "+N more" always renders | Directory.tsx (documents the fix) |
| AO-47 | **CLOSED by construction** — Inbox streams `listConversations` threads; Notifications streams the `notifications` collection. Different sources | Inbox.tsx / Notifications.tsx |
| AO-53 | **CLOSED by construction** — Inbox is ONE `listConversations` callable, no 4-stream load-gate | Inbox.tsx |
| AO-51 | **CLOSED by construction** — record-payment has no client date field; `markInvoicePaid` server-stamps | InvoiceDetail.tsx |
| AO-55 | **CLOSED by construction** — Communicate Recent shows real per-send engagement counts (`listRecentSends`) | Communicate.tsx |
| AO-50 | **N/A / satisfied** — the only kinfolk picker (AddKinDialog household) is a `<select>` dropdown; admin has no booking-search field | AddKinDialog.tsx |
| AO-45 | **N/A** — Home is a stub; the mis-routed dashboard widgets were never ported (widgets are separate unbuilt work AO-35..41) | Home.tsx |
| AO-46 | **not-a-defect** — no dynamic category chips; Notifications is day-grouped | Notifications.tsx |
| AO-54 | **by-design** — 17 real admin screens grouped into 3 nav groups; hiding shipped screens to hit "6" would be worse | AppShell.tsx |
| AO-52 | **mostly-moot** — FormSchemaEditor validates via `validateSchema` ported verbatim from the backend Zod contract, so backend `validationErrors` is only a contract-drift race | FormSchemaEditor.tsx |
| AO-26 | **correction** — the `kinfolk` collection has NO createdAt/updatedAt (joinDate only) and `kin` gets only `updatedAt`; "Recently Created" no-ops for kinfolk. A backend DATA gap (already disclosed in `api/directory.ts`), not a React bug. Remains open as data work. | api/directory.ts |

---

## 0. Closed THIS SESSION (2026-07-16) — do not re-log

| AO/ID | What | Evidence |
|---|---|---|
| AO-3 | git init both trees | AuntieOS `5f4622b`; MyTribe `2f636e7` (956 files, PII-scrubbed) |
| W25 | `scheduleDigestCron` dropped digest recipients past `.limit(1000)` | now drains via `paginateQuery`; cronPagination.test.ts 9/9 |
| W34 | divergent root `reconcile_comms.py` twin (old C-5 double-merge, missing note channel) | deleted; test repointed to canonical; py 3+10 green |
| W42 | `JvmFirestoreRest.getDoc` swallowed undecodable docs as silent null | now fails loud (System.err + Sentry), jvm compile green |
| N48 | paid `generate` endpoint had no per-uid cost cap | `generateRateLimit.js` daily cap + 429; funcs 72/72 |
| AO-9 | wasm crashes reported blind (`reportError` never called from commonMain) | seam `reportingExceptionHandler`/`rememberReportingScope` + wasm `initCrashReporting()` gap fixed + test; broad screen adoption OUTSTANDING (see AO-9 below) |
| AO-19 | `serviceTone` "sit" ∈ "visit_60" painted every visit Purple | **already fixed last session, re-verified**: both trees use `SIT_WORD = Regex("""\bsit""")`; `visit_60`→Orange. Stale in the plan doc. |
| C2 | test-admin outward re-tag on shared collections | **verified genuinely fixed**: MyTribe rules split `testOwnsIncoming`/`testOwnsExisting` on all six scoped collections |

---

## 1. OPEN — PERMANENT (android / desktop / backend). Real work; survives A8.

| AO | Was | Item | file:line | sev | surface |
|---|---|---|---|---|---|
| AO-18 | plan | `nowIso()` is UTC on web, LOCAL on desktop → after 19:00 CDT "today" is wrong. **ANDROID part FIXED 2026-07-16** (`KinCareSessionsScreen.kt:122` Auntie Time day-window now uses `LocalDate.now()`, not UTC `nowIso()`; android invoices/home/activity already used `LocalDate.now()`, so they were never affected; android compile green). **Web = throwaway** (A8; wasm `nowIso` must NOT be "fixed" to local — it would write local times to Firestore). **Desktop (jvm) still writes LOCAL to storage** (the inverse defect, owner-personal, low priority). The full commonMain split `nowInstantUtc()` (persist) vs `todayLocal()` (display) is deferred; **the React port must not inherit the ambiguity.** | android done; `web/.../util/Platform.*` remains | ~~HIGH~~ → med (android closed) | desktop + React-req |
| AO-21 | W8 | FCM acts on unauthenticated data pushes; client writes inbound call/vm/sms from spoofable payloads (feature-gate defaults OFF = vulnerable). Fix pairs with WARNING-8 webhook activation (operator). | `android/.../MainActivity.kt:185,216,242`; `AuntieFirebaseMessagingService.kt:24` | HIGH | android |
| ~~AO-22~~ | punchlist A6 | ~~android "Configure Review Booster" = empty lambda (dead control)~~ — **FIXED 2026-07-16**: built `ReviewBoosterEditorScreen` (per-platform Google/Yelp/Facebook enable + URL, mirrors MoodOptionsEditorScreen; model + `updateReviewBoosterConfig` already existed) + `Screen.KinTaleReviewBoosterEditor` route; the lambda now navigates to it. android-only (web has just the toggle + is throwaway; React builds its own). | `android/.../kintales/ReviewBoosterEditorScreen.kt` | ~~med~~ done | android |
| AO-23 | punchlist A4 | android template Edit may be swallowed by `AuntieEntityRow` outer `.clickable` (needs live-drive to confirm) | `android/.../ui/components/AuntieEntityRow.kt:105` | low | android |
| ~~AO-24~~ | punchlist W8-W13 | ~~android dashboard-widget parity: 5 widgets shipped on web, android enum stops at HEAT_INDEX~~ — **FIXED 2026-07-16**: ported DashboardInsights.kt (kotlinx.datetime→java.time) + 5 widget composables (HomeInsightWidgets.kt) + 5 DashKey enum entries (tokens match web so layouts round-trip) + labels + HomeScreen render branches + VM allSessions/kin data. android compile green; DashboardInsightsTest (12 cases) mirrors web. | `android/.../home/DashboardInsights.kt` + `HomeInsightWidgets.kt` | ~~low~~ done | android |
| AO-25 | RB-38 | `createMultiDateBookingRequest` — non-consecutive multi-date + recurring create (today single-date only) | `BookingScreen.kt:878` | med | all |
| AO-26 | RB-32 | directory Recently-Created/Updated sort falls back to alphabetic. **Corrected 2026-07-16**: the BACKEND already writes kin `createdAt`/`updatedAt` (addKin/updateKin/archiveKin) — the "no timestamp fields yet" comment was stale (fixed). Remaining is client plumbing (decode the Timestamps into the Kin model + getMyKin contract, then wire the sort). **Web surface is throwaway (A8); android has NO sort menu (DirectoryScreen.kt:68).** So this is now purely a **React-port requirement** — the data is ready. | `DirectoryScreen.kt` (web, throwaway) | React-req | React only |
| AO-27 | RB-63 / WA-44 | notifications stream not filtered by `recipientUid` server-side (rule now admin-permits, so no PERMISSION_DENIED, but the client reads the whole collection) | `FirestoreInterop.wasmJs.kt:351`; consumed `App.kt:233`, `InboxScreen:115`, `NotificationsScreen:106` | med | all (bridge permanent) |
| AO-28 | RB-65 / WA-45 | `NotificationEntry` lacks body/description + actor name/photo (dispatcher fields + UI); read-flag/target already added | `FirestoreClient.kt:2336` | med | all |
| AO-29 | WA-36 / RB-59 | `activity_log` listener has no server `orderBy`/`limit` → unbounded read of an append-only collection | `FirestoreInterop.wasmJs.kt:347`; `firebase-bridge.js:81` | med | bridge (permanent) |
| ~~AO-30~~ | WA-48 | ~~`assignTemplate` clobbers a custom `triggerKey` back to `catalogKey` on any merge re-assign~~ — **FIXED 2026-07-16**: `triggerKey` is now written only when the caller supplies one (merge preserves a custom value); dispatch keys off the doc-id `catalogKey` and ignores `triggerKey`, so new bindings are unaffected. assignTemplate.test.ts 6/6, tsc clean. | `MyTribe/.../assignTemplate.ts` | ~~low~~ done | backend |
| AO-31 | punchlist D1 | dossier reconcile prompt still injects dated citations `[[source: <ch> on YYYY-MM-DD]]` (clients strip at render; backend prompt unchanged) | `web/functions-python/reconcile_prompts.py:78,114` | low | backend (python) |
| AO-32 | W11 | `getTrainingDoc`/`getDraft` return the ENTIRE doc on shared-secret only, no field filter (superseded if AO-4 kills n8n endpoints) | `web/functions/index.js:262,283` | med | backend |
| AO-33 | N46/N47/N49 | n8n endpoint hardening: timing-unsafe secret compare, unbounded `n8nIpAllowed.timestamps` array, raw body forwarded to webhook. **Resolve by DELETION via AO-4, not hardening.** | `web/functions/index.js:188,168-183,572` | low | backend |
| AO-5 | plan | duplicate `generate` implementations (AuntieOS `index.js`/`generate.js` vs MyTribe `aiCopy.ts`) — one brand-voice source | both trees | med | backend (A5/A7) |
| ~~AO-6~~ | plan | ~~web app never registered as its own Firebase Web app~~ — **STALE/RESOLVED**: `auntieos-admin/src/lib/firebase.ts` documents the AuntieOS Web app IS registered (appId `1:153396971788:web:c2631409...`, verified 2026-07-15); the wasm bridge already uses it. The plan's comment was left behind after registration. | — | — | — |
| AO-7 | plan | AuntieOS-owned functions: 72 tests cover PURE HELPERS only; no exported handler exercised (`setAdminClaim`, `signCloudinaryUpload`, `searchMapbox`, `sendMessage`) — extract injectable cores | `web/functions/*.js` | med | backend |
| AO-8 | plan | cross-app contract types live only MyTribe-side; the shared package should be the single source | A1 | low | shared |

### Unbuilt dashboard widgets (all platforms; W14/W15 need a new backend model)

| AO | Was | Widget | Needs |
|---|---|---|---|
| AO-35 | punchlist W1 | Route Optimizer | `optimizeRoute` callable |
| AO-36 | punchlist W3 | Key & Code Safebox | — |
| AO-37 | punchlist W4 | "Don't Forget" Care Flags | — |
| AO-38 | punchlist W6 | Unread Client Messages | — |
| AO-39 | punchlist W12 | Expiration Countdown | — |
| AO-40 | punchlist W14 | Expense Quick-Log | expense model/collection |
| AO-41 | punchlist W15 | Supplies Tracker | inventory model/collection |

---

## 2. OPEN — WASM-THROWAWAY (the wasm admin is deleted at the React cutover "A8")

Owner ruling: do NOT polish the wasm admin. Fix only what actively MISLEADS the
operator between now and cutover, and carry every item as a **requirement for the
React rebuild**. `⚠` = actively misleading, worth a pre-cutover fix.

| AO | Was | Item | file:line | ⚠ |
|---|---|---|---|---|
| AO-10 | plan | Home rendered a failed read as `Open bookings: 0` (silent zero) — **FIXED not deployed** (`statCardValue` helpers + `StatCardStateTest`) | `HomeScreen.kt` | ⚠ |
| AO-42 | WA-26 | invoice detail (`invoiceStatusFor`) ignores free-text `status`; list (`invoiceIsOutstanding`) honors it → the two disagree on paid/outstanding | `InvoiceDetailState.kt:44` vs `InvoiceFilters.kt:39` | ⚠ |
| AO-43 | fix-backlog 12.1 | web "Attach file" passes an empty `ByteArray(0)` — the browser file-picker bytes are never wired (web upload silently uploads nothing) | `TrainingDocumentsScreen.kt:595` | ⚠ |
| AO-11 | plan | Directory card `.height(196.dp)` clips the "+N more" overflow chip → pets silently hidden (shows 2 of 4) | `DirectoryScreen.kt:451` | ⚠ |
| AO-12 | plan | AuntieOS admin shows an unredeemed CREDIT as PAID (with Receipt button); portal shows CREDIT. Negation-inferred status. **The A1 shared-primitives argument.** | `InvoiceFilters.kt:42` | ⚠ |
| AO-13 | plan | error banner AND empty-state render simultaneously (Tribal Intel, Templates) | structural `if(error)`+`if(empty)` | ⚠ |
| AO-14 | plan | 10–50s of skeletons before the app admits a read failed (failure is the slowest path) | Firestore listener retry | ⚠ |
| AO-44 | fix-backlog 0.3+4.10 | blur validation not wired on the flagship directory form (errors only gate on save) | `KinfolkEditScreen.kt:448-459` | |
| AO-45 | fix-backlog 2.2+2.3 | Home "review tales" + "KinTales to review" route to Communicate, not KinTales (one-line `onNavigate` bug ×2) | `HomeScreen.kt:328,407` | |
| AO-46 | fix-backlog 10.2 | notifications not organized by fixed business taxonomy (chips built from whatever categories exist) | `NotificationsScreen.kt:302` | |
| AO-47 | fix-backlog 10.3 | Inbox vs Notifications duplication (both render the same `notificationsStream()`) | `InboxScreen.kt:162` | |
| AO-48 | fix-backlog 4.1 | kinfolk profile is one long scroll; no tabs / quick-jump | `KinfolkEditScreen.kt:341` | |
| AO-49 | fix-backlog 14.3 | FormSchema editor has a manual "Field key" input, no snake_case auto-gen from label | `FormSchemaEditorScreen.kt:507` | |
| AO-50 | punchlist B10 | kinfolk selector is search-only, no dropdown (android already has `AuntieDropdownField`) | `BookingScreen.kt:822` | |
| AO-51 | WA-31 | record-payment Date field has no YYYY-MM-DD validation | `InvoiceDetailScreen.kt:589` | |
| AO-52 | WA-58 | client `saveSchema` drops backend `HttpsError details.validationErrors` | `FormSchemaRepository.kt:149` | |
| AO-53 | WA-33 | Inbox waits for all 4 streams before any row renders (load-gate; error-blanking already fixed) | `InboxScreen.kt:196` | |
| AO-54 | WA-62 | nav rail is ~20 items / 3 groups vs the curated 6 / 2 in the mockup | `NavDestinations.kt:34` | |
| AO-55 | RB-35 | Communicate "Recent open/read rates" has no metrics source | `CommunicateScreen.kt:114` | |
| AO-56 | RB-67 | `unassignTemplate` callable missing (delete/unassign not possible) | `TemplateAssignmentScreen.kt:344` + callable | |
| AO-57 | RB-40 | integration health is a static `CONFIGURED` label (mostly moot; n8n retiring) | `SettingsScreen.kt:2085` | |
| AO-15 | plan | wasm canvas exposes no accessibility tree (React restores it for free) | — | |
| AO-16 | plan | `AuntieGlass` rim hardcoded `Color.White` → white halo on every glass surface in dark | `AuntieGlass.kt:39` | |

### Data-coverage / by-design / model-fidelity (web-audit; mostly NOT bugs — verify data, don't "fix" code)

| AO | Was | Item | file:line |
|---|---|---|---|
| AO-58 | WA-7/8/9 | invoice model lacks `payments[]`/`dateIso`; Linked Sessions empty until `backfill_structural_links.py` PASS A5 runs | `InvoiceDetailScreen.kt:167`; `FirestoreClient.kt:2181` |
| AO-59 | WA-17 | "Unnamed Kinfolk" shown for orphan docs with empty `kinfolkName` | `BookingScreen.kt:606` |
| AO-60 | WA-19 | Auntie-time filter drops DRAFT/PENDING + off-window sessions (coverage choice) | `KinCareSessionsScreen.kt:281` |
| AO-61 | WA-21 | Schedule current-week grid empty when no session has a current-week `startTime` (data) | `ScheduleScreen.kt` |
| AO-62 | WA-28 | middle tile is "Billed total" (Σ totals), not "Paid this month" (no `paidDate` to sum) | `InvoicesScreen.kt:426` |
| AO-63 | WA-56 | KinTale-report Photos/GPS/per-pet names render placeholders (`mediaFileIds`/`visitRouteId`/`kinId` unresolved joins) | `KinTaleReportScreen.kt` |
| AO-64 | WA-37 | activity-log screen assumes ISO-string `timestamp` (now warns visibly; depends on writer type) | `ActivityLogScreen.kt:280` |
| AO-20 | plan | `visit_60` vs `60Minute` are two data sources (session docs vs `serviceRates` map), not a missing mapping — needs a mapping AND key-space reconciliation (product decision) | `ScheduleScreen.kt:255` |

---

## 3. BLOCKED — EXTERNAL SECRET or OPERATOR/OWNER decision (NOT engineering)

| AO/O | Item | Blocker |
|---|---|---|
| **A2P 10DLC** | brand + campaign registration | **the one true external blocker**: Twilio console, operator identity, fee, carrier review. No code unblocks it. |
| Twilio audio | record + upload 6 assets to `twilio-service/assets/` | operator records human voice |
| Twilio webhooks / WARNING-8 | point inbound webhooks at deployed `twilioInbound`, set its 3 URL env pins; give `incomingMessage` a destination in `studio_flow_v2.json` (today `next` is null → texts vanish) | operator Twilio console |
| AO-2 / W37 | root `.env` secret rotation (Baserow token, shared-secret) — now gitignored, rotation still owed | operator (largely mooted by AO-4 n8n deletion) |
| W36 | `android/serviceAccount.json` live admin key on disk (gitignored) | operator key rotation |
| RB-5 / RB-44 | Google Calendar busy-block sync + grid stream | external Google Calendar OAuth client secret |
| RB-6 | per-integration Connect/OAuth | external OAuth client secret |
| ~~AO-17~~ | ~~consolidate 17 raw corner radii to a real scale~~ — **RESOLVED 2026-07-16 (owner: consolidate)**: `auntieos-admin` `tokens.css` now has ONE 7-step radius scale (tight 4·chip 6·card 8·card-lg 12·panel 16·lg 20·pill) + the raws kept as deprecated round-to-nearest aliases; added the missing motion tokens (dur/ease). Done before the React screen fan-out so screens port onto a stable scale. Commit `3ee30dc`. | done |
| punchlist A3 | is "KinCare types/rates = web-only" acceptable, or does android need the `serviceRates` editor? | owner ruling |
| plan 5.1 | android tap-swallow: should the "Nblk" strip select the day or open the blocked slot? | owner product answer (Android is permanent → real work either way) |
| AO-4 | n8n kill-list COMPLETE + evidence (0 traffic 3 months); Phase B deletes the 3 endpoints + `N8N_SHARED_SECRET` | operator confirms no live n8n workflow targets them |

---

## AO-9 status (this session)

**DONE + compile-verified (wasm + jvm):**
- commonMain seam `reportingExceptionHandler()` / `rememberReportingScope()` +
  a test-observable `errorSink` (`CrashReporter.kt`) — routes an uncaught
  coroutine failure to `reportError` while it is still a typed Kotlin throwable,
  instead of escaping to `window.onerror` as `[object WebAssembly.Exception]`.
- the missing wasm `initCrashReporting()` call added to `main.kt` (was desktop-only).
- the handler wired into the **five long-lived `CoroutineScope(SupervisorJob())`
  view-model scopes** that host the persistent Firestore listeners — including
  `DirectoryViewModel:79`, the scope behind the live `AUNTIEOS-ADMIN-11` Directory
  crash — plus the two App composition-root scopes.
- `ReportingExceptionHandlerTest` (commonTest) pins the routing.

- **the full screen sweep is DONE**: all 33 per-screen `rememberCoroutineScope()`
  call sites now use `rememberReportingScope()` (34 files incl. App.kt), so every
  coroutine launched from a screen or a view-model routes an uncaught failure to
  Sentry. The only remaining `rememberCoroutineScope` in the tree is the seam's
  own implementation in `CrashReporter.kt`.

**Remaining:** runtime verification is still owed when/if the wasm admin ships
(force a throw behind a debug affordance, confirm a real Kotlin frame in Sentry)
— a green unit test cannot prove it, since the bug IS the missing call. No deploy
this session (wasm, owner ruling).

## Notes on de-duplication

- **AO-27 = RB-63 + WA-44**, **AO-28 = RB-65 + WA-45**, **AO-29 = WA-36 + RB-59**,
  **AO-33 = N46+N47+N49**, **AO-24 = punchlist W8..W13** — each was logged
  separately in two or three source docs; collapsed here.
- **AO-12 (cross-app admin-vs-portal) and AO-42 (within-AuntieOS detail-vs-list)**
  are two faces of the same disease (invoice status inferred by negation instead
  of enumerated). The React port's shared `invoiceStatusInfo` closes both.
- Every "already-fixed" claim the source docs still carried was dropped after
  verification; see each agent's reconciliation for the FALSE-claim lists.
