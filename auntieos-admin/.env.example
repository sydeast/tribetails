# AuntieOS admin: local/build environment.
#
# WHAT DOES *NOT* BELONG HERE
# Every real server secret lives in Google Secret Manager and is bound per
# function via `secrets: [...]` in the function's options. That includes
# SMTP2GO_API_KEY, EMAIL_FROM, TWILIO_*, STRIPE_*, TRIBE_PIN_PEPPER,
# WEB_API_KEY, AUNTIE_OPERATOR_UIDS, SENTRY_DSN, MAPBOX_ACCESS_TOKEN and
# CLOUDINARY_*. Do not copy any of them into a .env file: a function reads the
# bound secret, not this file, so a value here would be silently ignored while
# looking authoritative.
#
# REMOVED 2026-07-23, do not re-add
# The Baserow block (BASEROW_URL/TOKEN/EMAIL/PASSWORD/JWT/REFRESH/DATABASE_ID
# and the five BASEROW_TABLE_* ids) and N8N_URL/N8N_TOKEN: both services were
# retired and the only readers left were root-level Python scripts that are
# themselves dead. AUNTIE_SERVER_HOST/PORT and AUNTIE_SHARED_SECRET went with
# them: they had zero consumers anywhere in the repo, in any language.

# ANTHROPIC_API_KEY / ANTHROPIC_MODEL were removed 2026-07-23 along with the 19
# root-level Baserow/n8n Python scripts that were their only readers. Every
# deployed Anthropic path uses Secret Manager: web/functions binds the key, and
# web/functions-python reads its own directory or its binding. Do not re-add
# them here.

# --- Web admin (Vite / React) ---
# Sentry crash reporting for the React admin at auntie.tribetails.com. OPTIONAL:
# leave empty and reporting stays disabled (the app logs
# "Sentry disabled: no VITE_SENTRY_DSN" and runs normally). To activate, paste
# the client DSN from the Sentry project (Settings > Client Keys / DSN) at
# https://auntieos-ttpc.sentry.io.
#
# The VITE_ prefix is REQUIRED, and it is also why this cannot live in Secret
# Manager. Vite INLINES import.meta.env.VITE_* into the JavaScript bundle at
# BUILD time; there is no server process to inject a secret into at runtime, so
# the value has to be present when `vite build` runs. It then ships inside the
# bundle every browser downloads.
#
# That is fine, because a Sentry DSN is a public client key by design, not a
# server secret. It only permits write-only event ingestion into the project.
# Abuse is handled by Sentry-side rate limits and inbound filters, not secrecy.
#
# UNSET IN PRODUCTION as of 2026-07-23, which is why a two-hour CRITICAL error
# loop in notificationBatchSweep raised no alert. Set it and rebuild.
VITE_SENTRY_DSN=

# Optional. Tags events with a release so Sentry can group regressions and show
# which deploy introduced one. Any stable string; the build sha is the usual
# choice.
VITE_SENTRY_RELEASE=
