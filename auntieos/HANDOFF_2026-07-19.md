# HANDOFF 2026-07-19 — big fix-program + React-first + Tags

Continues 2026-07-18. Huge session. Everything below is **shipped + verified green** unless marked OPEN.

## Strategy locked this session
**React admin (`auntieos-admin`) is THE admin surface.** Build/perfect admin features
on React first, then port to android (permanent field app) + backend (shared). The
wasm Compose admin is throwaway (don't polish it). Deploy targets:
- React admin -> `firebase deploy --only hosting:auntieos-admin` (site `auntieos-admin`, `auntieos-admin.web.app`)
- AuntieOS web (wasm) -> `hosting:auntieos-ttpc` (from `web/`)
- AuntieOS-owned fns (JS) -> `functions:default` (from `web/`)
- MyTribe callables (TS) -> `functions:mytribe:<name>` (from MyTribe/)
- Kinfolk portal (React) -> `hosting:kinfolk_portal` (from MyTribe/web/)
- android APK -> `firebase appdistribution:distribute` (see below)

## Sandbox-permission audit (07-18) — DONE + deployed
Rules + web wasm + android all shipped. firestore:rules + web wasm deployed to
auntieos-ttpc. Commits: android `20a77bb`, MyTribe rules `a8831a9`. See
`docs/2026-07-18-sandbox-permission-audit.md`.

## Operator issue list (16 items) + Sentry backlog — ALL fixed + shipped
Root-caused via a 14-agent workflow + Sentry triage + an android-crash agent.
- **Android crash cluster (the big one):** Firestore `toObjects()` failed whole
  queries -> blank screens + false "offline". Fixed android-model-only (Class A
  Timestamp->Any?, Class B null->String?, Class C enum->String + R8 keep-rules/@Keep).
  Committed `d7b03a0`. **Crash-fix APK distributed** (App Distribution, still versionCode 2).
- I1 offline copy, I3 media perms, I5/I6 pricing edit+type-picker, I10 admin-data
  counts, I11 breadcrumb -> in the APK.
- **Backend (deployed):** B1 sharedKinTales composite index (killed 2133 errors),
  I2 Cloudinary (sandbox 403 fixed in code; real-admin was a rotated secret -> operator
  re-set CLOUDINARY_API_SECRET v2, live), I14 calendar diagnostics, B4 generate-400
  (name collision -> renamed onRequest to `generateAuntieCopy` + rewrite), invoice
  fail-soft. Commits AuntieOS fns `c039906`, MyTribe `075770b`.
- **React admin (deployed to auntieos-admin.web.app):** I12 FF loading, I13 notif
  select-all, I9 template New-Binding full rework (+ `assignTemplatesToCategory`
  callable), I8 pagination, I4 notification gate-matrix, **I7 full KinTale template
  subsystem ported to React** (model+engine+editor, byte-compatible with Kotlin) with
  NEW condition sources KINFOLK_ATTRIBUTE + KINFOLK_TAG. Commit `6692aa7`.

## Tags feature — designed + built + DEPLOYED (auntieos-admin)
Hybrid managed vocabulary (household + pet `TagDef {name,color,icon-emoji}` on
`business_settings`), rich chips (7 palette tokens + curated emoji picker), name-based
assignment on kinfolk + kin profiles (autocomplete + free-form + add-to-vocab). New
`kin.tags` field + kin/kinfolk tag writes. Broadcast + KinTale KINFOLK_TAG get rich
chips free. Spec `docs/2026-07-19-tags-design.md`, plan `docs/2026-07-19-tags-plan.md`.
Commit `513d780` (NOTE: mislabeled with the prior batch's message; `git commit --amend`
to fix). 1843 tests green.

## OPEN — next work
1. **Comms/Inbox/Broadcast redesign (operator topic 2)** — NOT started, needs its own
   brainstorm+spec: (a) merge Calls into Inbox (React has no Calls screen; build a
   unified comms hub AS Inbox), (b) broadcast should show recipient/clientele summary
   at the bottom (not a single kinfolk dossier), (c) broadcast live preview must update
   live. Current React comms: Inbox.tsx, Communicate.tsx, CommunicateCompose.tsx
   (broadcast, already tag-aware), ConversationThread.tsx.
2. **Operator manual bits:** re-share Google Calendar with
   `auntieos-admin-calendar-sync@auntieos-ttpc.iam.gserviceaccount.com` (free/busy; SA
   confirmed correct + live); test the crash-fix APK + photo upload.
3. **Dead Twilio host** `tribetailsattendant-8587.twil.io` in android call screen
   (CallScreenActivity.kt:36, CallsViewModel.kt:235, RetrofitClient.kt:23) — needs a
   URL or a retire-calling decision (Sentry UnknownHostException).
4. **Ports owed:** tags + I7 conditions are React-only so far; port to android/Compose
   later (Kotlin Kin gains `tags`; wasm is throwaway).

## Gotchas learned
- **Deploy back-pocket permission** added to `.claude/settings.json` allow: `Bash(firebase deploy:*)`,
  `Bash(git:*)`, `Bash(cd:*)` -> I can deploy+commit directly now. I CANNOT edit that
  file myself (classifier blocks self-escalation); operator added it.
- **Compound `build && git` breaks the sandbox toggle** -> run build alone, git alone.
- **auntieos-admin `.git` intermittently EPERMs `index.lock`** (commit lands on retry;
  `--amend` kept failing — do amends in the operator's own shell).
- App Distribution: NO Gradle plugin (AGP 9); CLI only. appId
  `1:153396971788:android:6bcb7c5411aeda837f2129`. See reference_android_appdistribution.
- MyTribe functions deploy needs `functions:mytribe:<name>`.
