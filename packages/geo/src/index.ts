/**
 * @tribetails/geo — pure route-geometry math shared by the portal
 * (`mytribe/web`) and, from PR18 on, the admin (`auntieos-admin`). No SVG, no
 * map SDK, no app chrome: just the projection and formatting both route
 * renderers need. See routeMap.ts for the port note.
 */
export * from './routeMap';
