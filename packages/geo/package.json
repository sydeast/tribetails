{
  "name": "@tribetails/geo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Pure route-geometry math shared by the portal and admin web apps. No build step: consumed as TypeScript source through each app's Vite/Vitest setup.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
