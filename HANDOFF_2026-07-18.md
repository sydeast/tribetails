# HANDOFF - 2026-07-18

Continues `HANDOFF_2026-07-17_react-rebuild.md`. Source of truth for open work:
`docs/BACKLOG_2026-07-16.md`. Current status: `_reference/STATE_OF_THE_UNION_2026-07-18.md`.

## What shipped this session

**Dashboard widget bucket COMPLETE (AO-35..41), all fullstack + deployed.**
Seven Home dashboard insight widgets across React admin + Compose (web/desktop) +
android + MyTribe backend. Hidden by default (Customize > add on the Home
dashboard).

- AO-38 Unread Client Messages, AO-36 Key & Code Safebox (hand-built first).
- AO-37 Care Flags, AO-39 Expiration Countdown, AO-35 Route Optimizer (Mapbox),
  AO-40 Expense Quick-Log, AO-41 Supplies Tracker (built via a 5-agent
  contract-first fan-out: backend + React + Compose + android, disjoint files).

Commits: backend `76b541b` + drift-guard `ac6a77f` (MyTribe); React
`57b1d49`/`e50e607`/`cf161f1` (auntieos-admin); Compose+android
`4b73739`/`02bcb3a`/`88095df` (this tree). All three repos clean.

Backend (DEPLOYED LIVE): 8 admin callables + 3 admin-only collections
(`expenses`/`supplies`/`expirations`) in `firestore.rules`. Contract:
`MyTribe/functions/CALLABLE_CONTRACT.md`.

**Arch AO-5/8.**
- AO-8 DONE (drift guard, design Option C): `callableContract.test.ts` freezes
  widget-callable request shapes; `CALLABLE_CONTRACT.md` is the canonical source
  the three client mirrors build from.
- AO-5: the "duplicate generate" was a misdiagnosis (two different speakers,
  Auntie-authoring vs kinfolk-assist). Shared lexicon at
  `docs/2026-07-18-voice-lexicon.md`. Design doc:
  `docs/2026-07-18-AO5-AO8-shared-contract-design.md`.

## Verification (re-run this session, not trusted from agents)
MyTribe tsc + 38 new tests + suite 1521 · React tsc + suite 1615 ·
`:composeApp:jvmTest` 31 · `:app:testDebugUnitTest` 28 · JSON field names
cross-checked backend to frontends. React admin + backend functions + firestore
rules deployed live.

## Open / operator-gated (nothing else engineering-blocked)
1. **AO-21 (Twilio)** - inbound webhook activation. Operator action; secure-mode
   flip loses inbound call/vm/sms until live.
2. **AO-5 prompt convergence** - rewriting the two FROZEN AI prompts to
   regenerate from `voice-lexicon.md` busts the Anthropic prompt cache and edits
   live copy. Held for the owner's voice sign-off. The full shared-package
   (design Option A) is a repo-topology decision (private registry vs sync vs the
   shipped guarded-mirror).
3. **AO-23** - unconfirmed Compose child-clickable bug; needs a live-drive before
   any blind change.

## Operator follow-ups (optional, low urgency)
- Redeploy Compose web (`wasmJsBrowserDistribution`) + rebuild APK to surface the
  new widgets on those apps (code committed; hidden-by-default so no behavior
  change until toggled). React admin already live.
- The widgets read `expenses`/`supplies`/`expirations` which start empty; log
  a few via the widgets to see them populate. Route Optimizer needs household
  `serviceAddress` set to route a stop (else it lists the visit as unroutable).

## Next-session candidates
- Extend the AO-8 drift guard + `CALLABLE_CONTRACT.md` to the other ~26 mirrored
  callables (currently only the widget callables are frozen).
- Continue the React admin build-out per `docs/BACKLOG_2026-07-16.md`.
